# 电子健康证明查询页面 — 使用说明

## 文件清单

```
健康证明页面/
├── index.html        ← 主页面
├── server.js         ← 本地同步服务（上传图片后自动推送到 GitHub）
├── 启动服务.bat      ← 双击启动 server.js（无需手动输命令）
└── 使用说明.md       ← 本文件
```

## 线上地址

✅ **GitHub Pages：** https://2262094142-afk.github.io/health-cert/

源码仓库：https://github.com/2262094142-afk/health-cert

---

## 如何修改正面卡片图片（并同步给所有扫码的人看）

### 前提条件

本机需安装 Node.js 和 GitHub CLI（已登录）。

### 步骤

1. **启动本地同步服务**
   双击 **`启动服务.bat`**

   服务运行在 `http://localhost:3456`

   > 如果命令行提示找不到 `node`，是因为系统没有全局配置 node 路径。请使用本文件夹里的 `启动服务.bat`，它会自动调用正确的 Node 路径。

2. **浏览器打开管理页面**
   访问 `http://localhost:3456`（或用浏览器直接打开 `index.html`）

3. **进入编辑面板**
   点击页面底部「**体检预约**」→ 输入密码 **`1234`**

4. **上传图片并保存**
   - 点击「点击上传图片」选择你的健康证正面图
   - 点击「**保存修改**」
   - 系统会自动：
     ① 把图片写入 `index.html`
     ② `git commit` + `git push` 到 GitHub
     ③ GitHub Pages 自动重新部署（约 1 分钟生效）

5. **验证**
   用手机扫描页面上的二维码，确认显示的是更新后的图片。

### 如果推送到 GitHub 失败

- 检查 `gh auth status` 是否已登录
- 检查网络连接
- 手动执行以下命令推送：
  ```
  cd 健康证明页面
  git add index.html
  git commit -m "更新图片"
  git push origin main
  ```

---

## 工作原理

```
管理员上传图片 → 浏览器 POST 到本地 server.js
                      ↓
           server.js 把 base64 图片写入 index.html 的 DEFAULT_IMAGE 变量
                      ↓
           server.js 执行 git add + commit + push
                      ↓
           GitHub Pages 自动重新部署（约1分钟）
                      ↓
           ✅ 所有扫码的人看到更新后的图片
```

- **优先级**：localStorage > DEFAULT_IMAGE > 空状态
- 管理员本机：localStorage 优先（即时生效）
- 扫码用户：读取 DEFAULT_IMAGE���由服务器推送后的值）

---

## 如何部署到线上（首次或换仓库）

### GitHub Pages（永久免费，推荐）

1. 注册 GitHub 账号
2. 创建新仓库，上传 `index.html`
3. Settings → Pages → 选 main 分支 → Save
4. 获得 `https://用户名.github.io/仓库名/` 地址

### EdgeOne Pages（腾讯云）

1. 腾讯云账号实名 → EdgeOne Pages 控制台
2. 上传文件或关联 GitHub
3. 获得 `xxx.edgeonepages.com` 域名

### 阿里云 ESA Pages

1. 阿里云账号实名 → ESA Pages 控制台
2. 上传文件或关联 GitHub
3. 获得 `xxx.esa` 域名

---

## 修改密码

打开 `index.html`，搜索 `ADMIN_PASSWORD = '1234'`，把 `1234` 改成新密码。

## 修改二维码指向地址

打开 `index.html`，搜索 `2262094142-afk.github.io/health-cert`，替换为你的新地址。

---

生成日期：2026-07-27
最后更新：新增 server.js 自动同步功能
